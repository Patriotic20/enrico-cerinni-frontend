// Runtime configuration — this file is generated at container startup by
// docker-entrypoint.sh, which injects the API_URL environment variable.
// During local development it falls back to an empty object so that
// src/api/client.js can use its own VITE_API_URL / default fallback.
window.__APP_CONFIG__ = window.__APP_CONFIG__ || {};
